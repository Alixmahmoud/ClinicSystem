using System.Windows;

namespace Clinic.Client.Wpf;

/// <summary>
/// Interaction logic for MainWindow.xaml. Behaviour lives in <see cref="MainViewModel"/>;
/// the code-behind only initializes the view.
/// </summary>
public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
    }
}